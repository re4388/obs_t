related:
- [[2025_01_10_08_16 Mac 時代的筆記]]
- [[update 2025_01_12_17_00_02 to align to win]]
- [[win_idx]]


---

## intelliJ 常用
alt+i -> go to implement
alt+m (method) -> file structure
alt+o → code/override method
alt+h → type hierarchy → 可以用來看有哪一些 （可以用的）實現類
alt+g (graph) -> show diagram popup
alt+t -> (trigger) code complete
alt+e -> recent file
alt+\ -> run  
alt+eneter -> show context action
`alt + [`  -> move caret to text start
`alt + ]`  -> move caret to text end
alt+w  (wrap) - surround with

ctrl+g -> generate 
ctrl+p -> parameter info

debug
alt+f1  step over
alt+f2 force step in 
alt+f3 resune program



f1: search everything
alt+2: go to class
alt+s: find in file


alt + backspace: delete to word start 
ctrl + backspace: delete to line start

## 還可以利用的
alt+y


---

## window key


Wiindows + 方向鍵：調整視窗大小和最小化應用程式
Windows + . （或；）：快速開啟表情符號表  (or use 我已經 mapping 的 alt+; to open clipboard and 就可以看到選單)
Windows + P：變更多螢幕顯示模式（外接螢幕、投影也可）
Windows + D 回到桌面


chrome 
Alt +D 直接選取網址列

Ctrl + Shift + Esc：快速開啟工作管理員
終結電腦異常：Ctrl + Alt + Delete（Del）




## ctrl (win 10) (like cmd in mac)

- extend selection, shrink selection→ ctrl + up/down
- add selection for next occurrenc → ctrl +d
- start a new line (idea) / xxx (vscode) → ctrl + enter
- win10/idea/parameter info (default binding) → ctrl+ p
- ctrl+shift + left/right
    
    - win10 built in → select left /right
    - idea: select left, select right
    - vscode: curosr word right select and cursor word left select



## alt

> 位置等同 mac 的 cmd, after I use karabinar to switch cmd ↔ alt)

- enter
    - idea → show context actions
- `up`/`down`, move line up down
- copy line down (duplicate line or selection)
    - mac `alt+cmd+up/down`
    - win ctrl+alt+down
- ``` show all window in the same app
- 1
    - show project side bar

- 3, show terminal, show script in dataGrip
- 4,  jump to matching bracket
- 5, 不好按 NO USE
- 6 不好按 NO USE
- 7 idea → see structure
- 8
    - jump to source (用在 git 編輯後跳到原本的檔案，或是跳到外部檔案）
    - NO USE
- 9 不好按 NO USE
- 0 copy relative path
    - todo
- - copy abs path
    - todo
- = multiple curosor (clone caret below)
    - todo

- q
    - go back  (global use Mae 設定了)
    - win10: alt+q
- a
    - go forward    (global use Mae 設定了)
    - win10: alt+a
- z
    - toggle folding
- w
    - google search via keyboard mastro (改用 raycast 的 google search, 因為有還有提示和歷史紀錄)
    - space + alt → Kit
    - shift+ctrl+space → raycast
    - IDEA/ wrap around
- s
    - IDE search text in file
    - win10/idea → find in files
- x
    - mac → text snipper
    - win10 → powerToy 的 text extracter
- e
    - ~~search everywhere , go to files~~
    - ~~show recent file~~
    - ~~recent location~~
    - win10/vscode/go to file… → 先不設定，可以用 f1 拿掉 `>` 取代
    - wiin10/idea/ 先不設定，因為f1就可以兼具go to files的功能了
    - recent file
- d
    - go to declaration (idea)  go to definition (vscode)
- c
    - console.log below (intelliJ use 套件 logit, vscode use `print it`)
    - win10, idea, 晚點設定，可以先勤勞一點

- r
    - go to usage, go to reference
- f
    - quick fix (show context Actions in IntelliJ)
- v
    - pure paste
    - win10/ use powertoy to remap remove-foramting paste
- b
    - show bookmark (alt+m 是mark bookmark)
    - no use now → 很多時候，我覺得還是把 code 複製到筆記本來還比較方便
- y →
    - ~~run something, 看 IDE 配置, 目前讓他跑 run ts code, 需要跑一次才會生效~~
    - ~~目前改為 啟動 live template~~
    - no use now
- n
    - no use
- u
    - reformat code  (vscode: format document)
- j (global)
    - win10 /autohotkey ()
- k (global)
    - win10 /autohotkey []
- ,
    - show VCS, like git stash, unstack, commit…etc
    - no use for now
- `l`  (global)
    - win10 /autohotkey  {}
- `.`
    - show git graph
    - no use for now

- `\`
    - open terminal in current folder. , 看 IDE 配置,
    - no use for now
- p
    - go to project window
    - ray-cast → open jetbrain toolbar project recent window
    -  no use for now
- `;`  (global)
    - mac : global clipboard Maccy
    - win10: mapping to win+v, built in clipboard manager
- `/`  (global)
    - iterm2 hotkey window
    -  no use for now
- `[`   (global)
    - home
- `‘` NO USE
- `]`   (global)
    - end

## functional key (f1~f12)
- f1
    - menu(vscode default), ~~Find Action… (intelliJ)~~
    - ~~go to files…~~
    - ddd
    - win10/vscode, idea, obs → open command line menu
- f2 rename

f1 and f2 在 miniMax 鍵盤也可以作為切換電腦的熱鍵，不過要先去logi option 設定中調整為f1 and f2 的設定

拍照

- f3,
    - mac  use cleanShot x → 預設自動打開 annotation-tool→ 可以編輯
    - win10 → `snipaste`
- f4, full screenshot mac 內建
    - use cleanShot x → 預設自動打開 annotation-tool(只能跟上面綁定)→ ctrl+w關掉
    - win10 → shareX

- f5
    - no use
- f6
    - no use
- f7
    - no use
- f8
    - no use
- f9
    - no use
- f10 no use
- f11, 12 , insert →  根據鍵盤上的圖案，使用autohotkey 去 mapping

聲音

- f10~f13: follow the keyboard symbol (mini MX)
## ctrl+shift


## ctrl `^`


d -> idea (add selection for next occurence)   vscode(add selection to next find match)

## vscode


this need to disable
這樣你的 win and mac 就不會互相影響，彼此只會 sync 自己的熱鍵
![[Pasted image 20250114173954.png]]

## chrome
- q → increase speed
- a → decrease speed
- cltr+shift+t -> reopen closed tab

- go to top and go to down
use below chrome extension to help 
![[IMG-keyboard_idx-20250115104803295.png]]




## obsidian
- 因為 obs 沒有內建 go to top and go to down 的熱鍵，讓我 remap
- 目前解法就是用 maestro
	- 但是上下又會和其他 app 衝到
	- 因此 obs 自己 duplicate global 設定
	- 另外 alt + 左右中框號自己 reamp



## mac keyboard software
[[karabinar]]
mac use shift to micmic ctrl+space to toggle input source(輸入法) -> [\[Mac\] macOS 中英文輸入切換 & 設定成跟 Windows 一樣用 Shift 切換教學 – OneJar 的隧道](https://www.onejar99.com/mac-keyboard-en-zh-switch-by-karabiner-elements/) -> 然後有小調整一下



## cmd (mac only) cmd is mac 常用功能鍵，小心覆蓋到default功能， like save, copy..等等

> (caps lock is also cmd via karabinar)

- 0~9 use IDEA bookmark/mnimonic system
    
- d, add selection for next occurrence (vscode:default, need add it in intelliJ)
    
    - 瀏覽器 copy tab → 使用套件 duplicate tab
- enter, start new line(intelliJ) , （vscode內建)
    
- d, add selection for next Occurrence(intelliJ) , （vscode內建)
    
- up and down arrow key, extend selection and shrink slection (intelliJ)
    
    - expand region and undo expand region(vscode)
- q, exit(default)
    
- a, select all (default)
    
- z, undo(default)
    
- w, close tab(default)
    
- s, save(default)
    
- x, cut(default)
    
- c, copy(default)
    
- r, refresh(default)
    
- f, find(default)
    
- v, paste(default)
    
- b (bold for text in markown or in many editor)


